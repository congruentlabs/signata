# @signata/web

The Signata Bridge dApp. Where holders import credentials, view their
attestations, and revoke if needed.

## Status

Skeleton. Real flows arrive as `@signata/verifier` and `@signata/sdk`
become usable.

## Development

```bash
pnpm --filter @signata/web dev
```
